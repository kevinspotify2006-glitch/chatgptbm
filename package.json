{
  "name": "business-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BUSINESS MANAGER — a browser-based business empire management simulator.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.1.0"
  }
}
